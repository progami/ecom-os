{
  "name": "@targon/config",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -w -p tsconfig.build.json",
    "type-check": "tsc --noEmit -p tsconfig.build.json",
    "lint": "echo 'No lint configured for config package'"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "zod": "^3.24.4"
  }
}
