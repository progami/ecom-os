import type { Config } from 'tailwindcss';
/**
 * Shared Tailwind preset for all TargonOS apps
 * Provides consistent brand colors, fonts, and semantic tokens
 */
export declare const targonPreset: Partial<Config>;
