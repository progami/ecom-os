#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Validate version consistency across monorepo
node scripts/sync-versions.js --check

# If versions are out of sync, exit with error
if [ $? -ne 0 ]; then
  echo ""
  echo "💡 Tip: Run 'node scripts/sync-versions.js' to sync all versions"
  echo ""
  exit 1
fi
