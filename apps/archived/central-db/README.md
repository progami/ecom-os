# Central DB (Archived)

This folder is archived and excluded from pnpm workspaces and CI/CD.

If you still have local environment files for the old Central DB app (for example `apps/central-db/.env.production`), keep them here instead.
